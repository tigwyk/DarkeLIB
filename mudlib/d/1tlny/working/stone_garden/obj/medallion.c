//  Wizard:  Shade Maelstorm
 // A medallion
 
#include <std.h>

inherit OBJECT;
 
void create() {
  ::create();
    set_name("medallion");
 set_id( ({ "medallion" }) );
 set_short("medallion");
 set_long("This is a shining black medallion, with the carving of a rose embedded in it");
    set_weight(20);
    set_value(5);
}

